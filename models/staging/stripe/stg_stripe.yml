version: 2

models:
  - name: stg_payments
    description: one unique customer per payment
    columns:
      - name: payment_id
        description: the primary key for stg_payments
        tests:
          - unique
          - not_null